import java.util.ArrayList;

public class MyMain {

    /**
     * Counts the number of words that starts with 'a' or 'A'
     * @param list A non-null, non-empty ArrayList of strings that are individual words containing only letters.
     *             No words will be null or empty.
     * @return The number of words starting with 'a' or 'A'. Min: 0, Max: <code>list.size()</code>.
     */
    public static int startsWithA(ArrayList<String> list) {
        // REPLACE WITH YOUR CODE
        return 0;
    }

    /**
     * Adds a ! to the end of each element in the ArrayList.
     * @param list A non-null, non-empty ArrayList of strings. No words will be null or empty.
     * @return The same list passed to the method.
     */
    public static ArrayList<String> makeExciting(ArrayList<String> list) {
        // REPLACE WITH YOUR CODE
        return list;
    }

    /**
     * Returns true if there is an int that appears in both ArrayLists
     * @param list1 An ArrayList of ints. Neither null nor empty.
     * @param list2 An ArrayList of ints. Neither null nor empty.
     * @return true if at least one int appears in both ArrayLists, false otherwise
     */
    public static boolean checkDuplicates(ArrayList<Integer> list1, ArrayList<Integer> list2) {
        // REPLACE WITH YOUR CODE
        return false;
    }

    /**
     * Counts the number of odd numbers in list
     * @param list An ArrayList of distinct ints. Neither null nor empty. May include negative numbers.
     * @return How many odd integers there are in the list.
     */
    public static int countOdd(ArrayList<Integer> list) {
        // REPLACE WITH YOUR CODE
        return 0;
    }

    /**
     * Takes an int[] as input and returns the equivalent ArrayList&lt;Integer>
     * Unlike the other methods, the input is an array!
     * @param arr An array of integers. length > 0.
     * @return A new ArrayList&lt;Integer> that holds the same integers as <code>arr</code> in the same order
     */
    public static ArrayList<Integer> convertToArrayList(int[] arr) {
        // REPLACE WITH YOUR CODE
        return null;
    }

    /**
     * Takes two <i>sorted</i> ArrayLists and merges them together into one big sorted ArrayList
     * (Hint: you may find it useful to use a while loop, as well as the remove() method).
     * You <i>can</i> change the contents of list1 and list2, but if you want a challenge, try to avoid it.
     * @param list1 A sorted ArrayList of integers. Neither null nor empty.
     * @param list2 A sorted ArrayList of integers. Neither null nor empty.
     * @return A new ArrayList&lt;Integer> with all integers from list1 and list2 in ascending (sorted) order
     */
    public static ArrayList<Integer> merge(ArrayList<Integer> list1, ArrayList<Integer> list2) {
        // REPLACE WITH YOUR CODE
        return null;
    }



    public static void main(String[] args) {
        // Write some code here to test your methods!
    }
}
